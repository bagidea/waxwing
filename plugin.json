{
  "id": "wax-wallet",
  "name": "🪙 waxwing",
  "version": "0.5.1",
  "description": "waxwing — self-custody Antelope wallet (WAX · EOS · Telos · XPR/Proton, testnet & mainnet, config-driven; default WAX testnet). v0.5.1: updated preview screenshots (real 4-tab panel: Overview·Resources·Send·Sign). v0.5.0: wallet-level password, auth lifecycle, auto-lock, encrypted export/import.",
  "brand": {
    "note": "Single source of truth is the BRAND const in index.js (the panel fetches /plugin/wax-wallet/brand). The `name` above is just the office-shelf label and mirrors BRAND.name.",
    "name": "waxwing",
    "logo": "🪙"
  },
  "panel": "panel.html",
  "window": { "w": 540, "h": 800, "resizable": false },
  "commands": [
    { "name": "networks", "args": "", "desc": "List the configured networks (WAX · EOS · Telos · XPR/Proton, testnet/mainnet) + which is selected" },
    { "name": "network", "args": "", "desc": "Show the currently selected network" },
    { "name": "setnetwork", "args": "<id>", "desc": "Switch network: wax-testnet | wax-mainnet | eos-testnet | eos-mainnet | telos-testnet | telos-mainnet | xpr-testnet | xpr-mainnet (default wax-testnet)" },
    { "name": "chaininfo", "args": "[--network <id>]", "desc": "Ping a network's RPC — chain id, head block, server version" },
    { "name": "account", "args": "<name>", "desc": "Look up an account on the selected network: balance + CPU/NET/RAM" },
    { "name": "balance", "args": "<name> [SYMBOL] [contract]", "desc": "Token balance (default eosio.token / core symbol of the selected chain)" },
    { "name": "history", "args": "<name> [limit]", "desc": "Recent transactions of an account (Hyperion v2)" },
    { "name": "keyaccounts", "args": "<publicKey>", "desc": "Look up which on-chain accounts a public key controls (Hyperion get_key_accounts)" },
    { "name": "create", "args": "<password> [account] [permission]", "desc": "Generate a NEW keypair — auto-resolves on-chain accounts via Hyperion; single account→auto-bind, multiple→return list, none→manual setaccount" },
    { "name": "import", "args": "<password> <privkey> [account] [permission]", "desc": "Import a private key — auto-resolves on-chain accounts via Hyperion (same as create)" },
    { "name": "accounts", "args": "[--network <id>]", "desc": "List stored keys/accounts on the selected network (public info only — never private keys)" },
    { "name": "overview", "args": "[--network <id>]", "desc": "Multi-account dashboard: every stored account on the selected network with its OWN live balance + CPU/NET/RAM (each separate, not one combined blob)" },
    { "name": "unlock", "args": "<password> [account]", "desc": "Decrypt key(s) into memory after verifying the password (auto-locks)" },
    { "name": "lock", "args": "", "desc": "Wipe all decrypted keys from memory" },
    { "name": "status", "args": "", "desc": "Wallet status: selected network, networks, stored accounts, locked/unlocked, brand" },
    { "name": "setaccount", "args": "<select> <account> [permission]", "desc": "Bind a stored key to an on-chain account name / permission" },
    { "name": "newaccount", "args": "<json>", "desc": "Create a NEW on-chain account on the selected network, bound to a generated key (creator pays RAM+stake)" },
    { "name": "select", "args": "<account>", "desc": "Pick the active account (default sender) on the selected network" },
    { "name": "send", "args": "<json>", "desc": "Build a sign-intent for a transfer (from/to/quantity/memo) — returns a summary to confirm; does NOT broadcast yet" },
    { "name": "confirm", "args": "<id>", "desc": "Broadcast ANY pending sign-intent (transfer or resource) after the user taps Sign (requires unlocked wallet)" },
    { "name": "cancel", "args": "<id>", "desc": "Discard a pending sign-intent (transfer or resource; user dismissed the Sign popup)" },
    { "name": "stake", "args": "<json>", "desc": "Stake CPU/NET (delegatebw) {from,receiver?,cpu?,net?} — build a sign-intent; confirm broadcasts" },
    { "name": "unstake", "args": "<json>", "desc": "Unstake CPU/NET (undelegatebw) {from,receiver?,cpu?,net?} — ⚠️ tokens locked 3 days; confirm broadcasts" },
    { "name": "buyram", "args": "<json>", "desc": "Buy RAM {from,receiver?,bytes?|quantity?} — bytes=buyrambytes, else token spend; confirm broadcasts" },
    { "name": "sellram", "args": "<json>", "desc": "Sell unused RAM {from,bytes} — tokens back immediately; confirm broadcasts" },
    { "name": "remove", "args": "<select>", "desc": "Delete a stored key from the keystore (current network)" },
    { "name": "config", "args": "[autoLockMs=N]", "desc": "Show or set auto-lock duration in ms (min 30000, default 600000 = 10 min)" },
    { "name": "export", "args": "<password>", "desc": "Export the ENCRYPTED wallet (scrypt→AES-GCM, never plaintext) — safe to save as a backup file" },
    { "name": "importwallet", "args": "<password> <data>", "desc": "Restore a wallet from an encrypted export blob — decrypts and merges byNet/accounts/selected" },
    { "name": "brand", "args": "", "desc": "Return the brand name/logo (single source of truth)" }
  ],
  "needsKeys": [],
  "enabled": true
}
